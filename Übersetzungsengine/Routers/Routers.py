import Router

class Routers:
    def __init__(self):
        self.accesspoints : list[Router.Router] = []

    def getDevices() -> list[Router.Router]:
        return
    
    def getDevice(pProductNumber : str) -> Router.Router:
        return

    def translate(pProductNumber : str) -> Router.Router:
        return
    
    def updateDevice(pProductNumber : str, pRouter : Router.Router):
        return
    
    def loadData():
        return